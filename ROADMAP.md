# ロードマップ — Memolette Flutter版

## 設計方針（Swift版から引き継ぎ・絶対的命題）
- **UIは常にシンプルを保つ** — 機能が増えてもカオスにしない
- 普通の人にもものすごく使いやすく、パワーユーザーの期待も大きく上回る
- 「鳥肌が立つほど便利！」と言わせるアプリにする
- タブ内のメモ一覧は「バッグ」と呼ぶ（開発用語として統一）

## Phase 1: コア機能移植
- [ ] Driftセットアップ（テーブル定義、マイグレーション）
- [ ] Memo CRUD（作成・読取・更新・削除）
- [ ] 自動保存（入力即保存、debounceなし）
- [ ] Riverpodプロバイダー構成

## Phase 2: タグ管理
- [ ] Tag モデル（親子2階層、56色パレット）
- [ ] Memo ↔ Tag 多対多リレーション（中間テーブル）
- [ ] タグ作成・編集・削除
- [ ] タグ履歴（最大20件）

## Phase 3: メモ一覧画面
- [ ] グリッド表示（タグごとのグリッドサイズ可変：小/中/大）
- [ ] フォルダタブ表示（親タグ＝タブ）
- [ ] 特殊タブ（すべて、タグなし、よく見る、TODO）
- [ ] ピン固定メモの上部表示
- [ ] メモ検索
- [ ] タブの左右無限ループスクロール
- [ ] タブのドラッグ並び替え

## Phase 4: メモ入力・編集画面
- [ ] テキスト入力エリア（最大50,000文字）
- [ ] タイトル編集
- [ ] ルーレット（扇形UI、親子タグ選択）
- [ ] Undo/Redo（最大50スナップショット）
- [ ] メモ詳細画面（全画面表示・編集）
- [ ] 確定ボタン（キーボード閉じるだけ）
- [ ] 入力欄の最大化/縮小

## Phase 5: 爆速メモ整理モード
- [ ] フィルタ選択（タグなし/タイトルなし/古いメモ/すべて）
- [ ] カルーセルUI（1件ずつ表示、左右フリック）
- [ ] ルーレットで即座にタグ付与
- [ ] タイトル・本文のインライン編集
- [ ] 上フリック削除（放り投げアニメーション）
- [ ] 完了時の戦績表示
- [ ] 50件ごとのセット処理

## Phase 6: マークダウン対応
- [ ] MDフラグ管理 + 自動判定（containsMarkdown）
- [ ] リアルタイムプレビュー
- [ ] 記号入力ツールバー（H1-H3、太字、斜体、リスト等）
- [ ] Bear風インラインマークダウン
- [ ] MDトグル（初回説明ダイアログ付き）
- [ ] URL自動検出リンク（閲覧モードのみ。下線表示、タップでブラウザ起動。url_launcher使用）

## Phase 7: ToDo機能
- [ ] ToDoリスト CRUD
- [ ] ToDoアイテム（階層構造、完了、期限）
- [ ] チェックボックス操作
- [ ] ドラッグ並び替え

## Phase 8: iPad対応（★優先／同期テストの前提）
ユーザーが iPad しか所持していないため、Phase 9 同期検証の前に必須。
B（スプリットビュー）で実用価値を出してから C の要素を段階的に追加する方針。

### Step 0: 事前課題（着手前に潰す）
- [x] 横画面回転は実機で動作確認済（iPad 実機 iOS 26.2.1）。シミュレータは既知バグで回転追従しないため、開発用に Info.plist `UIRequiresFullScreen=true` を一時的に有効化してシミュで回転動作を取り戻している。**Phase 8 完了後に外すこと**（Split View / Slide Over / Stage Manager を復活）
- [x] 設計判断は確定済
  - 縦画面 iPad: iPhone 同等の単列レイアウト（Step A 実装済）
  - スプリットビュー: 横画面のみで固定比率（1:1 + `VerticalDivider`）
  - タブレット判定: `isTablet = shortestSide >= 600`、`isWide = width >= 840 && width > height`

### Step A: レスポンシブ基盤（B の土台）
- [ ] タブレット判定ユーティリティ（`isTablet`, `isWideLayout` helper）
- [ ] MediaQuery / LayoutBuilder でブレークポイント設計
- [ ] コンテンツの max-width 制限（入力欄・フッターツールバーを中央寄せ）
- [ ] メモグリッド列数の動的化（iPad幅で 3〜4 列）
- [ ] 絶対値→相対値化（ROADMAP Phase 14 から前倒し）

### Step B: スプリットビュー（横画面の主役）
- [ ] 横画面 or 幅広時は左右2カラム
  - 左: タブ＋バッグ一覧／右: メモ詳細
  - Swift版 MainView.swift を参考に設計
- [ ] 1メモ編集中も一覧が見えたまま編集できる動線
- [ ] Multitasking / Stage Manager の動的リサイズに追従（起動中にも画面幅が変わる。最大の盲点）
- [ ] iPadOS Split View / Slide Over での破綻チェック

### Step C: iPad らしさの付加価値（優先度順）
- [ ] **⌘キーショートカット** — `⌘N` 新規、`⌘F` 検索、`⌘Z`/`⇧⌘Z` Undo/Redo、`⌘B/I` 太字/斜体、`⌘1-9` タブ切替
- [ ] **ドラッグ&ドロップ** — 他アプリから画像/テキストを投げ込んで即メモ化、メモ→他アプリへのエクスポート
- [ ] **右クリック / 長押しコンテキストメニュー** — 編集・削除・タグ付け
- [ ] **サイドバー常時表示** — 親タグ一覧を左固定（NavigationRail 的な佇まい）
- [ ] **並列編集モード** — 画面を左右分割して別メモを並べる（アイデアメモから昇格）
- [ ] **Apple Pencil Scribble 対応** — ブロックエディタ内で TextField が正しく受け付けるか検証
- [ ] **マルチウィンドウ（iPadOS Scene）** — 同じアプリを2画面で開く（余力があれば）

### UI 微調整（実機スクショで見つけた破綻）
- [ ] 入力エリアが横長すぎて空白多い（max-width 制限）
- [ ] メモカードが iPhone と同じ2列で間延び（列数動的化）
- [ ] サブフィルタ（すべて/よく見る/最近/タグなし）が等間隔分散で横に広がりすぎ
- [ ] フッターツールバーが左寄り、右半分が空白
- [ ] 全体的な間延び感 — 余白と最大幅を制御（フォントサイズは固定でOK）
- [ ] ルーレットUIの配置 — iPad だと腕動作が大きくなる。片手の親指が届く位置に寄せる
- [ ] タブの無限スクロール — iPad幅だとタブが余る。見せ方を再設計

### 盲点 / 要検証
- [ ] フローティングキーボードで `viewInsets.bottom` が 0 でも隠れるケース
- [ ] BTキーボード接続時のショートカット・フォーカス挙動
- [ ] トラックパッド hover / secondary tap
- [ ] Safe Area / Home indicator が縦横切替で動的変化
- [ ] 画像サムネの密度（画面が広い分、小サムネが点のように見える恐れ）

### 完了条件
- [ ] iPadシミュレータ（Pro 12.9 / 11 / mini / Air）で破綻なく動く
- [ ] iPad 実機でビルド＆動作確認（ケーブル入手後）
- [ ] 縦横切替、Split View、Stage Manager でも崩れない

## Phase 9: 同期

### ★★★ データ保護方針（同期実装の必須要件・絶対遵守）★★★
他アプリで「ローカル→クラウド移行で全データ消失」の事故事例あり。
**バグは許容できるがデータ消失だけは死んでも起こさない**。同期機能は以下を全て満たさない限りリリース不可。

#### 設計原則
- **同期は「複製」、移行ではない**：ローカル DB は **絶対に消さない**。クラウドは常にコピー先
- **片側破損で全滅しない**：ローカルが壊れても、クラウドが壊れても、もう一方から復旧できる前提に
- **デフォルト挙動は最も保守的に**：迷ったら何もしない。書き換え／削除より「読み出し専用で別場所にコピー」

#### 多重防御策（実装必須）
- [ ] **マイグレーション/初回同期前の自動スナップショット**：SQLite ファイルそのものを `memolette.sqlite.bak.YYYYMMDD-HHmm` で別ファイルにコピー。失敗時は自動ロールバック
- [ ] **2-phase commit**：先にクラウドへアップロード → サーバー側で件数・ハッシュ検証 → 検証通った時点で「同期済み」マーク。ローカルデータの削除/上書きはしない
- [ ] **件数・ハッシュチェック**：アップロード前後で `SELECT COUNT(*)` と全レコードのハッシュ比較。1 件でもズレたらアボート＋ロールバック
- [ ] **ユーザーに見える定期エクスポート**：1 日 1 回、JSON/SQLite を Documents ディレクトリ（iOS Files から見える場所）に書き出し。アプリが死んでもユーザー自身で救出可能
- [ ] **アンインストール耐性**：iCloud Drive 等、アプリ削除しても残る場所にバックアップを配置
- [ ] **マイグレーション前の必須確認 UI**：「○件のメモをクラウドへ。バックアップは Documents/memolette-backup-XXXX.db に保存しました」と明示。ユーザー承認なしには進まない
- [ ] **アプリ内ロールバック UI**：「設定 > データ復元」で過去のバックアップから戻せる
- [ ] **テスト網羅**：移行ロジックはユニット＋統合テスト必須。テストなしでリリース不可

#### 同期機能本体
- [ ] Firebase連携（認証・Firestore）
- [ ] SQLite ↔ Firestore 同期ロジック（上記方針に厳格準拠）
- [ ] iCloud/CloudKit 同期（Phase 1 では見送り）
- [ ] オフライン対応
- [ ] コンフリクト解決（最終更新優先 + 競合履歴を保持）

#### 設計方針（採用）
- **マルチプラットフォーム前提で Firebase 一本化**（iOS/Android/Web 共通、iCloud は不採用）
- **基本機能はログイン不要・無料**でローカル SQLite 完結
- **Pro プラン（サブスク）+ ログイン**で同期機能解放
  - サブスク管理: RevenueCat 推奨（in_app_purchase 直叩きでも可）
  - 認証: Firebase Auth（Apple Sign-In / Google Sign-In / メール）
- **同期 ON にした初回**は「ローカル → クラウド」「クラウド → ローカル」をユーザー選択
- **ログアウト時はクラウド参照を切るのみ**、ローカル DB は維持

#### Firestore ドキュメント構造（暫定案）
```
users/{uid}/
  memos/{memoId}        — Memo （title, content, isPinned, bgColorIndex, eventDate, createdAt, updatedAt, _hash, _version）
  tags/{tagId}          — Tag （name, colorIndex, parentTagId, sortOrder, ...）
  todoLists/{listId}    — TodoList
  todoItems/{itemId}    — TodoItem （listId 参照）
  memoTags/{compoundId} — メモタグ多対多
  memoImages/{imageId}  — 画像メタ（バイナリは Cloud Storage 別管理）
  meta/sync             — 最終同期時刻、件数、ハッシュ全体値
images/{uid}/{imageId}.jpg — Cloud Storage（バイナリ）
```

#### 同期トリガー
- アプリ起動時 / フォアグラウンド復帰時 / DB 書込み後 N 秒（debounce）
- 失敗時はローカル変更を保留してリトライ
- ユーザーから「今すぐ同期」を強制実行できる UI

#### Step 進行
- [x] **Step 1: データ保護**（バックアップ・復元・エクスポート） — Phase 9 に先行して実装済み
- [ ] **Step 2: Firebase プロジェクト準備**（Console 作成 + flutterfire configure）
- [ ] **Step 3: Firebase Auth UI**（ログインしなくても使える状態を維持しつつ、ログイン UI のみ実装）
- [ ] **Step 4: サブスク導入**（RevenueCat or in_app_purchase）
  - **プラン設計（同期＝有料機能）**:
    - 無料: 同期不可、ローカルのみ
    - お試し1ヶ月: 同期可、画像100枚（約15MB）まで
    - Pro 月額500円: 画像7000枚（約1GB）
    - Premium 月額1500円: 画像7万枚（約10GB）
  - 容量カウント: Firestore に metadata 記録、アプリで「あと○MB」表示
  - 超過時は警告 → Pro 誘導
- [ ] **Step 5: 同期ロジック本体**（2-phase commit + ハッシュ検証）
- [ ] **Step 6: 競合解決 UI**（最終更新優先 + 競合履歴の閲覧）

## Phase 10: Android対応
- [ ] Android SDK セットアップ
- [ ] Android固有のUI調整
- [ ] テスト

## Phase 11: 画像・地図挿入
- [ ] メモへの画像挿入（カメラ/フォトライブラリ）
- [ ] 圧縮+リサイズ（長辺1024px, JPEG 70%, 1枚100-200KB）
- [ ] サムネイル遅延ロード
- [ ] 地図（MapKit/Google Maps）の埋め込み

## Phase 12: AI機能
- [ ] AIによるタグ付け・タイトル付け（提案→承認フロー）
- [ ] AI要約・相関図
- [ ] ユーザーカスタムプロンプト

## Phase 13: テーマ・アクセシビリティ
- [ ] テーマ切替（モダン/ポップ/モノトーン）
- [ ] ダークモード
- [ ] カラーブラインドモード
- [ ] 左利き対応

## Phase 14: リリース準備
- [ ] 多言語対応（日/英/中/西/仏）
- [ ] アプリアイコン
- [ ] サブスク実装（Pro版）
- [ ] 絶対値レイアウトの相対値化
- [ ] 商標チェック
- [x] **アクセシビリティ：文字サイズ対応 → アプリ内固定方針を維持**
  - 検討結果: システム文字サイズに追従させると個別の固定高さ Widget が大量に
    崩壊して調整コストが見合わないため、`main.dart` で `TextScaler.noScaling`
    によりアプリ内 1.0 固定を継続する。
  - 将来案: アプリ独自で「ノーマル / 大」の **2段階トグル** を設定画面に追加
    （スライダーや3段階以上は影響範囲が広すぎるため不採用）。
    実装時は `TextScaler.linear(1.15)` 程度で「大」を表現し、影響箇所
    （メモカードの行数計算、ToDoカードの高さ等）を個別に検証。

## Phase 15: カレンダービュー
日記や日付と紐付けたメモを作れるようにする。一覧した時に俯瞰しやすい。

仕様（2026-04-25 決定）
- 対象: メモ + ToDoリスト + ToDoアイテム個別 すべて
- 日付フィールド: `createdAt` とは別の `eventDate` (DateTime?) を新設。過去/未来日付に紐付けられる
- 表示単位: 「全カレンダー」特別フォルダ一つに集約（タグごとカレンダーは将来検討）

実装項目
- [ ] DB Migration v4: Memos / TodoLists / TodoItems に `eventDate` (DateTime?) 追加
- [ ] `GridSizeOption.calendar` を追加（or 特別フォルダ専用ビュー扱い）
- [ ] 「全カレンダー」特別タブをフォルダタブ群に追加（既存の「すべて」「タグなし」と同列）
- [ ] 縦スクロール月別カレンダー Widget 実装
  - 各日に件数バッジ + 「+」ボタン
  - 日付タップで当日アイテム一覧（メモ/ToDoリスト/ToDoアイテム混在）
- [ ] メモ入力UIに「日付指定」欄追加（任意・未指定可）
- [ ] ToDoリスト編集画面に「日付指定」欄追加
- [ ] ToDoアイテム編集に「日付指定」欄追加
- [ ] 「+」タップ → アクションシート（メモ作成 / ToDoリスト作成 / ToDoアイテム作成）→ 既存入力UIに `eventDate` プリセット渡し

## Phase 16: 全画面サイズ対応のレスポンシブ化（リリース前必須）

**背景**: これまで主に iPhone 15 Pro Max（430pt）で開発・検証してきたため、メモカード／ToDoカード／カレンダー UI 等が **絶対値レイアウト**で組まれており、画面幅が小さい機種（375pt: SE 2/3/12-13 mini、390pt: 16e、393pt: 17 Pro 等）で表示崩れ。CLAUDE.md の「絶対値レイアウト禁止 — 比率ベースで実装する」原則に立ち返って整備する。**縦幅も含む両軸対応**が必要。

### スコープ
- [ ] **メモカード**（`memo_card.dart`）の `_titleFont` / `_bodyFont` / `_cardPadding` 等を「カード実幅から派生する比率計算」に変更（`LayoutBuilder` で実幅取得）
- [ ] **ToDoカード**（`todo_card.dart`）も同様に比率化（しおりサイズ、タイトルフォント、件数バッジ等）
- [ ] **GridSizeOption** 各値（grid3x6/2x5/2x3/1x2/1flex/titleOnly）を画面幅に応じて適切に表示／メニュー項目側でも対応
- [ ] **カレンダー月間ビュー**（`calendar_view.dart` 内 `_MonthBlock`）の日付セル高さ・フォントを画面幅依存に
- [ ] **DayItemsPanel** のヘッダ／カードフォント・パディング
- [ ] **メモ入力エリア**（`memo_input_area.dart`）の高さ計算が縦幅（特に SE 系の 667pt）でも収まるように
- [ ] **検証マトリクス**: 全 gridSize × 全画面幅クラス（320/375/390-393/430+）で見た目すり合わせ

### 検証対象シミュ
- iPhone SE 3rd（375×667）— 最小サポート想定
- iPhone 13 mini（375×812）— 縦長 + 狭い
- iPhone 17 Pro（393×852）— 標準
- iPhone 17 Pro Max（430×932）— 最大
- iPad Pro 12.9" / 13" — タブレット系（既に対応済み、回帰確認のみ）

## Phase 17: Web版（PWA / ブラウザ対応）

**背景**: 既存の Flutter コードを最大限流用してブラウザでも動作するアプリを提供する。Phase 9 同期と組み合わせれば、iOS / Android / Web の3面で同じデータが見られる。

### スコープ
- [ ] **Drift の Web 対応**: `lib/db/database.dart` の openConnection を mobile/web 分岐。Web は IndexedDB バックエンド (`drift/wasm.dart`)
- [ ] **画像保存の Web 分岐**: `ImageStorage` を Web 用に再実装（IndexedDB or Firebase Storage）。`image_picker` の Web 対応版利用
- [ ] **Firebase Auth / Firestore の Web SDK**: `firebase_core_web` で初期化、認証 UI もそのまま使える
- [ ] **マウス・キーボード対応**: ホバー、右クリックメニュー、ショートカット（⌘N で新規メモ等）
- [ ] **ブラウザ固有の挙動チェック**: 戻る/進むボタン、タブ切替時のフォーカス挙動
- [ ] **レスポンシブ確認**: 超ワイド・狭幅両方で見栄え調整（既に Wide レイアウト対応済みなので楽）
- [ ] **Firebase Hosting デプロイ**: `flutter build web` → `firebase deploy --only hosting`
- [ ] **PWA 化**: manifest.json 整備、オフラインキャッシュ、ホーム画面追加対応

### 動かないもの
- iCloud / CloudKit（Apple 製は Web 不可）
- ファイルシステム直アクセス（IndexedDB 経由になる）

### 注意点
- Flutter Web の CanvasKit ビルドは初期ロード重め。 HTML レンダラーとの選択を検討
- SEO 不可（SPA）— ランディングは別途 HTML で

## Swift版の未実装ロードマップ（将来移植候補）
Swift版ROADMAPにあるが未実装の機能。優先度は要検討：
- タグサジェスト（3層構造: 事前辞書+ユーザー蓄積+最近の組み合わせ）
- メモリンク（メモ同士をつなぐキャンバスUI）
- タスクリンク（メモやタスクを枝のように派生）
- エクスポート（MD→HTML、メール、SNS投稿）
- スニペット登録
- 無限下書き機能
- アカウント・ログイン機能
- PC版（macOS/Web）

## 既知の問題・修正待ち
- ToDo項目入力中に「項目追加」ボタンを押すと、入力中の項目名が一瞬「空のアイテム」に変わってから確定される不具合（編集中アイテムの commit タイミングと新規追加の競合）

## 後回しにした機能
（現在なし）

## アイデアメモ
- メモ入力モード中の画像 × ボタン削除で編集モードを抜けない（カーソル復元して編集続行）
- 子タグドロワーの「都度収納」と「常時表示」を設定でオンオフ切替可能に（フォルダ切替時にドロワーを閉じるかどうか）
- アプリ内文字サイズ設定: 「ノーマル / 大」の2段階トグルを設定画面に。スライダーや3段階以上は影響範囲が広すぎる（カード行数・高さ計算が崩壊する）ため不採用。
- ToDoアイテムを別の親の下にドラッグ移動（階層レベルは維持、親だけ変更）
- 並列編集モード: 画面を上下に分割して別々のメモを表示・編集。上のメモを見ながら下のメモにまとめる等。画像も読み込み可能
- 他ユーザーとのメモの共有機能（要ログイン）
- Google Calendar 連携（予定の表示や登録もアプリ内から可能に）
- リマインダー搭載（メモ・ToDo に通知を紐づけ）
- 自分宛にタップ一発でメール送信機能（メモ内容や ToDo を自分宛にメール）
- メッセージやりとり機能、その中でメモや ToDo の共有も可能

## 備忘（次回相談）
- データ保護画面のダイアログ文言検討（バックアップ作成成功時 / Documents エクスポート成功時 / 復元前の確認 / 復元後の再起動案内）
- Firestore セキュリティルールを「テストモード」から本番ルールに書き換える（30日以内、Phase 9 同期実装と並行）
- データ保護画面の「復元」改修: 一覧から直タップではなく、「復元」ボタンを置いて押した先でバックアップ一覧→選択→確認ダイアログ、のワンクッション構成に
